#include <stdio.h>

int a(int n) {
    int sum = 0;
    for (int i = 1; i <= n; ++i) {
        sum += i;
    }
    return sum;
}

int main() {
    int n;
    printf("Enter a positive integer n: ");
    scanf("%d", &n);

    printf("Sum of 1 to %d is: %d\n", n, a(n));

    return 0;
}
